# Email Templates

Store all email template builders in this folder.

- Keep each template in its own file.
- Export template builders through `index.ts`.
- Keep `lib/emails/send.ts` for delivery only.
- Prefer inline styles in HTML for broad email-client support.

